language: node_js
node_js:
  - 14
cache: yarn
script: yarn build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: public
  target_branch: master
  on:
    all_branches: true
  fqdn: a13x.io
